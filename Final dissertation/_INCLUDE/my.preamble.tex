% Include common packages
\usepackage{graphicx}
\usepackage{caption}
% \usepackage{subfigure}
\usepackage{amssymb}
\usepackage{amsmath}
\usepackage{float}
\usepackage{subfig}
\usepackage{booktabs}
\usepackage{makecell}
% \usepackage{appendix}
\usepackage{textcomp}
\usepackage{pgfgantt}
\usepackage{pdflscape}
\usepackage{color}
\usepackage[numbers, sort&compress]{natbib}

% Insert SVG file
\usepackage[inkscapelatex=false]{svg}

% Set spacing
\usepackage{setspace}

% Set pdf properties
\usepackage{hyperref}
\hypersetup{hidelinks} 

% My symbols
\newcommand{\xor}{\oplus}

% Merge cells in a row
\usepackage{multirow}

% Set the position of the page number
\usepackage{fancyhdr}
\newcommand{\pageHeaderNumPos}[1]{
    \fancypagestyle{plain}{
    \fancyhf{}
    % Hide line
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0pt}
    \fancyhead[#1]{\thepage}}
    \pagestyle{plain}}

\newcommand{\pageFooterNumPos}[1]{
    \fancypagestyle{plain}{
    \fancyhf{}
    % Hide line
    \renewcommand{\headrulewidth}{0pt}
    \renewcommand{\footrulewidth}{0pt}
    \fancyfoot[#1]{\thepage}}
    \pagestyle{plain}}

% % Set font
% \usepackage{fontspec}
% \setmainfont{Times New Roman}

% Set draft watermark
\newcommand{\watermarkEnable}[1]{
    \usepackage{draftwatermark}
    \SetWatermarkText{#1}
    \SetWatermarkLightness{0.8}
    \SetWatermarkScale{7}
}

% % [NOT RECOMMENDED]Configue landscape/portrait pages with header and footer
% \usepackage[paper=portrait, pagesize]{typearea}
% \usepackage{pdfpages}
% \BeforeRestoreareas{\cleardoublepage}
% \newcommand*\uselandscape[2]{
%     \pagebreak
%     \cleardoublepage
%     \KOMAoptions{paper=landscape}
%     \recalctypearea
%     \areaset{#1\textwidth}{#2\textheight}% adjust here: \areaset{<text body width>}{<text body height>}
% }

% \AtBeginDocument{
%     \storeareas\normalpapersize
% }
% \newcommand*\useportrait{
%     \pagebreak
%     \normalpapersize
% }

% Rotate the table
% Ref: https://blog.csdn.net/haifeng_gu/article/details/108563975
\usepackage[figuresright]{rotating}

% % Indent control
% \setlength{\parindent}{0pt}    % don't indent first lines of paragraphs

% Keep tables in the same section
\usepackage[section]{placeins}

% Thousands separator
\usepackage{numprint}
\npthousandsep{,}
\newcommand{\num}[1]{\numprint{#1}}
% \usepackage[group-separator={,}]{siunitx}

% % Spaces for bin and hex
% \usepackage{fmtcount}
% \usepackage{trimclip,ifthen}
% \newsavebox\numberbox
% \newlength\boxoffset
% \newcommand\chop[2]{%
%   \bgroup\ttfamily%
%   \setbox0=\hbox{0}%
%   \savebox\numberbox{#2}%
%   \boxoffset=0pt%
%   \whiledo{\lengthtest{\boxoffset<\wd\numberbox}}{%
%     \clipbox{\boxoffset+0pt 0pt \wd\numberbox-\boxoffset-#1\wd0 -1pt}{\usebox{\numberbox}}%
%     \addtolength{\boxoffset}{#1\wd0}%
%     \ifdim\boxoffset<\wd\numberbox\ \fi%
%   }\unskip\egroup%
% }

% \usepackage{soul}
% \newcounter{binst}
% \makeatletter
% \newcommand{\FormatBinary}[1]{\begingroup%
% \setcounter{binst}{0}
% \def\SOUL@soeverytoken{%
% \stepcounter{binst}%
% \ifnum\value{binst}=5\relax%
% \setcounter{binst}{1}\,%
% \fi%
% \the\SOUL@token}%
% \so{#1}\endgroup} 
% \makeatother

% Double & single quotes
\newcommand{\dquotes}[1]{``#1''}
\newcommand{\squotes}[1]{`#1'}

% Copy version
% printVersion = 0 for softcopy; printVersion = 1 for hardcopy
\newcount\printVersion
\printVersion = 1
% Hardcopy
\newcommand{\enableHardcopy}{
}
% Softcopy
\newcommand{\enableSoftcopy}{
}

% Clear double-page
\newcommand{\myClearDoublePage}{
    \ifnum\printVersion=0
    % \cleardoublepage
    \else
        \cleardoublepage
    \fi
}

\newcommand{\addOddPage}{
    \ifnum\printVersion=0
    % \cleardoublepage
    \else
        \checkoddpage
        \ifoddpage
        \else
            \newpage
            \mbox{}
        \fi
    \fi
}

